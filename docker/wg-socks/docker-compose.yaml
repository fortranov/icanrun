services:
  wg-socks:
    build: .
    # hostname registers in Docker DNS so other containers on the same
    # network can resolve it as "icanrun-wg-socks" — works even when
    # Coolify overrides container_name with its own UUID-based value.
    hostname: icanrun-wg-socks
    cap_add:
      - NET_ADMIN
    sysctls:
      net.ipv4.ip_forward: "1"
      net.ipv4.conf.all.src_valid_mark: "1"
    environment:
      - WG_PRIVATE_KEY
      - WG_ADDRESS
      - WG_PEER_PUBLIC_KEY
      - WG_PRESHARED_KEY
      - WG_ENDPOINT
    # Bind ONLY to the Docker-internal coolify gateway (10.0.1.1).
    # This makes the proxy reachable inside the coolify network as
    # 10.0.1.1:1080 without exposing it on the public interface.
    ports:
      - "10.0.1.1:1080:1080"
    restart: unless-stopped
    networks:
      coolify:
        aliases:
          - icanrun-wg-socks

networks:
  coolify:
    external: true
